# JWT Secret for authentication
JWT_SECRET=replace_with_secure_secret

# Port for the server
PORT=4000

# Database: Using SQLite for local development
# For production (Railway), switch to PostgreSQL and set DATABASE_URL
